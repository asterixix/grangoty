// Tailwind CSS v4 uses the @tailwindcss/vite plugin instead of PostCSS.
// This file is kept for compatibility but Tailwind is NOT processed here.
export default {
  plugins: {
    autoprefixer: {}
  }
}