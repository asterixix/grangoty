import { defineNuxtRouteMiddleware } from 'nuxt/app'

export default defineNuxtRouteMiddleware(() => {
  // This middleware can be used for route protection or redirects
  // For now, it's a placeholder
})